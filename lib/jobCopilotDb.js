/**
 * DEPRECATED — replaced by api.jobs.* (lib/api.js → Express → Firestore).
 * Stubs kept so old references don't crash the build.
 */
export async function loadJobCopilotState() { return null; }
export async function saveJobCopilotState() { return false; }

