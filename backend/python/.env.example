# Copy to .env and fill in values.
# Python FastAPI — Data OS Portfolio Microservice

PORT=8000
DEV=1

# SMTP — Gmail app password (not your regular password)
# https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password
CONTACT_TO=nigamjob32@gmail.com

# Frontend origin (optional extra CORS origin)
EXTRA_ORIGIN=https://your-custom-domain.com
